import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    container: {
        position: "absolute",
        bottom: 0,
        backgroundColor: "#fff",
        width: "100%",
        padding: 15
    },
    none: {
        display: "none"
    },
    button: {
        color: "red",
        fontSize: 12,
        fontWeight: "500",
        marginLeft: 5
    },
    is: {
        fontWeight: "300",
        fontSize: 12
    }
});

export default styles;